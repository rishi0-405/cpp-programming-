#include <iostream>
using namespace std;

int main() {
    int myVar = 20;
    int* ptr = &myVar;

    cout << "Value of myVar: " << myVar << endl;
    cout << "Memory address of myVar: " << ptr << endl;
    cout << "Value retrieved using the pointer: " << *ptr << endl;
}
